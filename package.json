{
  "name": "runtime-watch",
  "version": "0.1.0",
  "private": true,
  "description": "Automated checks for runtime behavior across Node.js versions.",
  "type": "module",
  "scripts": {
    "check": "node scripts/check.mjs"
  }
}
